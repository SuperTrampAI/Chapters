dict,list,set,forzenset ,以及tuple，str这个类是用来存储unicode字符串的，而bytes和bytearray这两个类是用来存储二进制数据的

datetime 日期
    
calendar 日历

collections 容器

collections.abc 容器的抽象基类

heapq 堆队列算法

bisect 数组二分查找算法

array 数值数组

weakref 弱引用

type 动态类型创建和内置类型名称

copy 浅层和深层的复制操作。

pprint 数据美化输出

reprlib 另一种repr()实现

enum 枚举

